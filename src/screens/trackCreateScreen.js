import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

const TrackCreateScreen = () => {
  return (
    <View>
      <Text style={styles.size}>TrackCreateScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  size: {
    fontSize: 25,
  },
});

export default TrackCreateScreen;
